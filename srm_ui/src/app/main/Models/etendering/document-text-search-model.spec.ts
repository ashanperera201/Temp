import { DocumentTextSearchModel } from './document-text-search-model';

describe('DocumentTextSearchModel', () => {
  it('should create an instance', () => {
    expect(new DocumentTextSearchModel()).toBeTruthy();
  });
});
