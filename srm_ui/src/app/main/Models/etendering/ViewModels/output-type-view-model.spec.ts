import { OutputTypeViewModel } from './output-type-view-model';

describe('OutputTypeViewModel', () => {
  it('should create an instance', () => {
    expect(new OutputTypeViewModel()).toBeTruthy();
  });
});
