import { LogicalUnitViewModel } from './logical-unit-view-model';

describe('LogicalUnitViewModel', () => {
  it('should create an instance', () => {
    expect(new LogicalUnitViewModel()).toBeTruthy();
  });
});
